query AllJudges($limit: Int, $page: Int) {
    judges(limit: $limit, page: $page) {
        __typename
        id
        name
    }
}

query Judge($id: ID!) {
    judges_by_id(id: $id) {
        __typename
        name
        id
    }
}
